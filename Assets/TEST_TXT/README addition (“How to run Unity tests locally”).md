## How to run Unity tests locally
1. Open **Window → General → Test Runner**
2. Select **Edit Mode** or **Play Mode**
3. Click **Run All**
4. Save screenshots of the results to `TEST_RESULTS/screenshots/`
(Optional) Install **Code Coverage** package → Generate report → store under `TEST_RESULTS/coverage/`
